# Base URL of the ASP.NET Core backend's API.
# Leave unset to use the same-origin "/api" path via the dev proxy in vite.config.ts
# (which forwards to https://localhost:5001 — update that target if your backend runs elsewhere).
VITE_API_BASE_URL=https://localhost:5001/api
